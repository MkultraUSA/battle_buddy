name: BB Pipeline
on:
  workflow_dispatch:
    inputs:
      task:
        description: 'Goal'
        required: true
        type: string

jobs:
  coder:
    name: Hermes coder - open PR
    runs-on: ubuntu-latest
    steps:
      - name: Work logic placeholder
        run: |
          echo "Starting task: ${{ inputs.task }}"
          echo "This is where the SSH and Scp logic will eventually go."
